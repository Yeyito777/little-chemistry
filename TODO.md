# Current iteration

- [x] 1. Supply coherent text-only native entity reference bundles: texture pixels together with cuboids, pivots, and per-face UV mappings translated into the generated entity-model coordinate contract. Keep manual indexed-texture and exact-source search available for additional inspiration.
- [x] 2. Make vanilla bow/crossbow mechanics a good default rather than a restriction: expose composable generated control over use, charge/release, ammunition, projectile creation, consumption, durability, and impact while retaining the native route when selected.
- [x] 3. Add general generated multiblock assemblies with atomic placement/removal and synchronized state-dependent render and collision geometry, so authored openable structures can actually span blocks and become pass-through without concept-specific rules.
- [x] 4. Make ordinary workstation input/fuel slots player-extractable by default while preserving intentional per-slot customization and the existing temporary invention lock.
- [x] 5. Add focused mechanical regression tests for all four source-level fixes without adding capability details to the universal system prompt or initial selection query.
- [x] 6. Run the full test/build suite, install the exact artifact into the LittleChemistry Prism instance, and leave the iteration ready for in-game testing.

# Previous iteration

- [x] Diagnose generated entity UV orientation/wrapping with the Phantom Skiff as a regression case, then fix the general entity texture/model authoring or renderer contract.
- [x] Remove unintended projectile-ammunition restrictions and expose a composable native path for generated bows/crossbows to choose arrows, fireworks, or concept-specific projectiles.
- [x] Render every generated workstation and player-inventory slot with the exact vanilla slot palette, borders, highlights, and corner pixels.
- [x] Require generated workstations to define invention-in-progress and result-ready particle effects using vanilla or authored particles, and dispatch them from the engine-owned lifecycle.
- [x] Make portable-storage nesting a gentle native-style insertion rejection rather than deleting items, while retaining composable generated slot-policy customization.
- [x] Refactor generation context into a concise universal selection query plus focused post-selection capability contracts.
- [x] Add persistent Exocortex folder instructions and regression tests preventing shared prompt/API bloat and duplicated capability guidance.
- [x] Run the full test/build suite, install the exact artifact into the LittleChemistry Prism instance, and leave the iteration ready for in-game testing.
